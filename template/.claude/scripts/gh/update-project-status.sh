#!/usr/bin/env bash
# TODO: spec §3.2 — move an Issue/PR between Projects V2 columns via
# `gh api graphql`. Status names come from pipeline.json project_board.statuses.

set -euo pipefail
echo "update-project-status.sh is a stub — see spec §3.2" >&2
exit 64
