---
description: architect 에이전트를 호출해 spec → plan 으로 변환 (spec §4.2 패턴 A 2단계)
argument-hint: "<slug>"
---

<!-- TODO: spec §4.2 — architect 에이전트 호출하여
     .artifacts/specs/{slug}-spec.md → .artifacts/plans/{slug}-plan.md.
     plan 에는 REQ-N → task_id 매핑과 task DAG 가 포함되어야 한다. -->
