{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/seonghunlee94/ai-pipe/schemas/impl-agent-input.schema.json",
  "$comment": "$id is a stable identifier, not externally dereferenced; it points at the canonical repo.",
  "title": "Implementation Agent Input",
  "description": "Spec §11.1 — input contract for backend-eng, frontend-eng, infra-eng. additionalProperties:false catches typos like task_idd; declare new optional inputs in properties.",
  "type": "object",
  "required": [
    "task_id",
    "feature_branch",
    "task_branch",
    "story_number",
    "issue_number",
    "downstream_notes"
  ],
  "properties": {
    "task_id": {
      "type": "string",
      "pattern": "^T-[0-9]+$"
    },
    "feature_branch": { "type": "string" },
    "task_branch": { "type": "string" },
    "story_number": { "type": "integer", "minimum": 1 },
    "issue_number": { "type": "integer", "minimum": 1 },
    "short_name": { "type": "string", "maxLength": 8 },
    "task_title": { "type": "string" },
    "downstream_notes": {
      "type": "object",
      "description": "이전 task 로부터의 정보 (null 불가, 빈 object 는 허용)"
    },
    "downstream_notes_from_prev": {
      "type": "object",
      "description": "Spec §4.3 — 이전 task의 downstream_notes를 명시적으로 전달받는 필드 (선택)."
    }
  },
  "additionalProperties": false
}
