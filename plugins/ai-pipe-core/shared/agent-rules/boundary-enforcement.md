<!-- TODO: spec §6.1 — SSOT for boundary enforcement rules referenced by all
     impl agents via SHARED_REF blocks. Should cover:
       - Protected paths (same list as verify-boundary.sh)
       - Worktree isolation rules
       - Git push/force restrictions
       - Cross-agent file access rules
-->

# Boundary Enforcement (SSOT)

TODO: 작성 예정. spec §6.1, §7.2 참조.
