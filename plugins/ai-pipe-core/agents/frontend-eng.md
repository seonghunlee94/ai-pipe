---
name: frontend-eng
description: |
  Frontend Engineer. Implements UI per task with native worktree isolation,
  mirroring backend-eng's contract (spec §4.1, §6.1).
model: sonnet
isolation: worktree
tools: []
---

<!-- TODO: spec §4.1, §6 — adapt backend-eng.md for frontend.
     Input/output schemas identical to impl-agent-{input,output}.schema.json.
     isolation: worktree (native) already declared — no manual git worktree. -->
