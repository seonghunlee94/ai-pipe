---
name: backend-conventions
description: |
  Backend-specific implementation conventions (layered architecture,
  error handling, logging, DTO/entity separation, transaction boundaries,
  test layout). Status: STUB — paths auto-activation is intentionally
  disabled until real content lands (PR3+; partly auto-generated by
  `ai-pipe detect`). Planned paths: src/**/*.{ts,go,rs,py,java}.
disable-model-invocation: true
user-invocable: false
---

# backend-conventions

TODO: PR3+. 백엔드 구현 관례 SSOT. 다음을 다룬다:

- 레이어 분리 (controller → service → repository)
- 에러 처리 패턴 (도메인 에러 vs 시스템 에러, exception vs Result type)
- 로깅 규칙 (correlation ID, sensitive field redaction)
- DTO/Entity 분리 및 변환 위치
- 트랜잭션 경계
- 테스트 레이아웃 (unit/integration/e2e 분리)

PR4의 `ai-pipe detect` 명령이 프로젝트 스택을 감지해 이 파일의 일부 섹션을 자동 생성한다 (스펙 §3-2).
