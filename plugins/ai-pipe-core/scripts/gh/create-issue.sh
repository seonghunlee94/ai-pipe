#!/usr/bin/env bash
# TODO: spec §3.2 — wrapper around `gh issue create` with project-board
# linkage. Reads project_board.statuses from .claude/config/pipeline.json
# to set the new issue to the configured "backlog" status.

set -euo pipefail
echo "create-issue.sh is a stub — see spec §3.2" >&2
exit 64
